# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Raw geoBoundaries downloads cached by scripts/build_food_security_data.py
# — re-derivable by re-running the script, not worth committing (the CSV in
# source_data/ itself IS committed, for provenance, per user request).
source_data/admin1_raw

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
